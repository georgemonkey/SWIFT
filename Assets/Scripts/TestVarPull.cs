using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestVarPull : MonoBehaviour
{
    // Start is called before the first frame update
   void Start()
{
    
    Debug.Log(Variables.droneCount);
    Debug.Log(Variables.searchPattern);
   
}

    // Update is called once per frame
    void Update()
    {
        
    }
}
